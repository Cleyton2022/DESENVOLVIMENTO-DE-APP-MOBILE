import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'loja_delivery',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
